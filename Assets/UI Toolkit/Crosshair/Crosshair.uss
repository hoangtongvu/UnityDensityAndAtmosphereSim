#root {
    flex-grow: 1;
    justify-content: center;
    align-items: center;
}

#crosshair {
    width: 4px;
    height: 4px;

    background-color: rgba(255, 255, 255, 0.4);
    border-radius: 50%;
}